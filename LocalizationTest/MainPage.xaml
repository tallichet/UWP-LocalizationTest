<Page
    x:Class="LocalizationTest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LocalizationTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Name="self">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical"
                    HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Uid="LanguageTextBlock" FontSize="25" HorizontalAlignment="Center" />
            <TextBlock x:Name="LanguageValue" FontSize="25" HorizontalAlignment="Center" Foreground="Red" />
            <TextBlock x:Name="FromPCL" FontSize="25" HorizontalAlignment="Center" Foreground="Green" />
            <!--Text="{Binding ElementName=self, Path=PCLResources.PortableString}"-->

            <TextBlock x:Name="Langs" FontSize="15" Margin="20" HorizontalAlignment="Center" />
        </StackPanel>
    </Grid>
</Page>
